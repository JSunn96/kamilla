//Store text some way
//Make a container for the text on the HTML page
//Make the text change to click
 window.onload = function () {
     const anyad = document.getElementById("dynamicText");
     anyad.innerHTML = 'Változott a téma';

 };